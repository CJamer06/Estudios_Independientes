// Números

let a = 5;

console.log(a);

let b = 0.1;

console.log(b);

// Los numeros en JS solo se pueden declarar de esta manera, tanto enteros como los numeros decimales.

// - Precicicon

let c = 0.2;

console.log( b + c); // Aca se suele presentar un error por parte de JS a la hora de almacenar los datos.

console.log(Math.round((0.1 + 0.2)*100) / 100); // Un truco para solventar el error antes mencionado.